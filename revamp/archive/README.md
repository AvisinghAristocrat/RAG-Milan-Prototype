# Revamp archive

On 2025-11-03 we removed raw exports from git history because they contained sensitive tokens.
See `backup_manifest.json` for details. Sanitized copies may be committed using the suffix
`.sanitized.csv` and are allowed by .gitignore.

If you need the original exports for any reason, request access from the repo owner.
